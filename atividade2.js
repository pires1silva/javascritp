const  listadealunos=14;

for (let index = 0; index <= listadealunos; index++ ) {

if (index == 0) { 
    console.log('zero' + index);
} else if(index % 2 == 0)
    { console.log('par'+ index);}
  else 
    { console.log('impar' + index);}
}
